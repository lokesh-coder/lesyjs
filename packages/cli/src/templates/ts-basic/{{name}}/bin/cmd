#!/usr/bin/env node

const path = require("path");
const lesy = require("@lesy/compiler");

const options = {
  isTypescriptApp: true,
  root: path.resolve(__dirname, "../"),
};

exports.default = lesy(options).parse();
