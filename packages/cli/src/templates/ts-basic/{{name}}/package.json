{
  "name": "{{name}}",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "clean": "rm -rf ./dist",
    "build": "npm run clean && tsc --build tsconfig.json",
    "test": "jest"
  },
  "bin": {
    "{{cmdName}}": "bin/cmd"
  },
  "dependencies": {
    "@lesy/compiler": "^{{version}}"
  },
  "devDependencies": {
    "@lesy/testbed": "^{{version}}",
    "@types/jest": "^24.0.19",
    "@types/node": "^12.11.1",
    "copyfiles": "^2.1.1",
    "jest": "^24.9.0",
    "ts-jest": "^24.1.0",
    "typescript": "^3.6.4"
  },
  "keywords": [
    "lesy"
  ],
  "author": "",
  "license": "MIT"
}
